# 训练助手 PWA 设计文档

日期：2026-05-17
状态：已根据当前实现同步
当前实现目录：`/Users/joel.xue/Documents/tasks/fitness-training-pwa`
当前实现提交：
* `da5e1a9 feat: build fitness training pwa`
* `1d909ae feat: localize training app to Chinese`

## 1. 目标

构建一个手机端、中文界面的训练辅助 PWA，用来在训练时读取当天计划、显示当前动作/重量/目标次数、自动开启组间休息倒计时、记录实际次数，并支持导出训练记录。

这个工具的定位是“训练执行器 + 本地记录器”，不是完整健身教练系统。它不会自动修改训练计划，也不会根据表现自动加重量。

## 2. 当前产品决策

* 平台：手机浏览器优先，主要面向 Android Chrome PWA。
* 技术栈：Vite + React + TypeScript。
* 存储方式：local-first，使用 IndexedDB，本地保存计划、历史训练和未完成训练。
* 计划来源：支持导入 JSON，也内置一份当前示例训练计划。
* 界面语言：用户可见页面文案以中文为主，CSV / JSON 作为文件格式名保留。
* 训练入口：用户手动选择训练日，例如 力量A、力量B、力量C、有氧。
* 提醒方式：休息结束时尝试声音 + 震动，同时页面显示“休息完成”状态。
* 导出方式：支持当前训练或全部历史导出为 CSV / JSON。
* 部署方式：静态站点，可部署到 Cloudflare Pages、Vercel、Netlify、GitHub Pages 或 Firebase Hosting 等 HTTPS 静态托管平台。

## 3. 版本 1 非目标

* 不做账号系统。
* 不做云同步。
* 不做 App Store / 原生 App 发布。
* 不做复杂训练计划编辑器。
* 不做自动加重量或自动调计划。
* 不做饮食记录。
* 不做图表化数据分析。
* 不接入手表、心率带等可穿戴设备。

## 4. 当前用户流程

1. 用户在手机浏览器打开已部署的 HTTPS 地址，或开发阶段打开局域网预览地址。
2. 如果本地没有计划，首页提示导入训练计划，也可以加载内置示例计划。
3. 首页显示当前计划名称、版本和可开始的训练日。
4. 用户选择某一天训练。
5. 如果是力量训练，进入训练执行页，显示当前动作、组数、组类型、计划重量、目标次数和目标肌群。
6. 用户完成一组后点击“完成本组”。
7. 系统立刻开始休息倒计时，并让用户输入或修正该组实际次数。
8. 倒计时结束后，系统尝试播放声音、震动，并显示休息完成。
9. 用户点击“开始下一组”继续。系统不会自动跳转到下一组，因为健身房环境可能需要用户自己确认准备情况。
10. 最后一组完成后，系统进入总结页，保存本次训练，并允许导出 CSV / JSON。
11. 用户可以在历史页查看以往训练、导出全部记录或清空本地数据。

## 5. 当前核心页面

### 5.1 首页

已实现内容：
* 中文标题：训练助手。
* 副标题：离线训练记录。
* 导入训练计划按钮。
* 加载示例计划按钮。
* 当前计划名称和版本。
* 训练日按钮列表。
* 最近一次训练摘要。
* 进入历史 / 导出的入口。

如果没有训练计划，页面显示“先导入计划”和简短说明。

### 5.2 力量训练页

已实现内容：
* 当前训练日名称。
* 当前动作名称。
* 当前组序号。
* 中文组类型：顶组、降重组、正式组、辅助组、有氧。
* 计划重量。
* 目标次数。
* 目标肌群标签。
* 动作备注。
* 主按钮：完成本组。

完成一组后：
* 自动进入休息倒计时。
* 显示实际次数输入框。
* 休息结束后按按钮允许进入下一组。
* 最后一组结束后按钮显示“完成训练”。

### 5.3 有氧页

已实现内容：
* 当前有氧训练日名称。
* 有氧项目名称，例如 爬坡跑步机。
* 目标时长。
* 目标心率。
* 实际时长输入。
* 心率范围输入。
* 备注输入。
* 完成有氧后保存为一次训练记录。

### 5.4 总结页

已实现内容：
* 力量训练：展示每组动作和实际次数。
* 有氧训练：展示项目、实际时长、实际心率和备注。
* 展示简单进阶提示。
* 导出当前训练 CSV。
* 导出当前训练 JSON。
* 返回首页。

当前版本没有显示总训练时长，也没有“漏做组”统计。

### 5.5 历史页

已实现内容：
* 按时间倒序显示历史训练。
* 力量训练显示完成组数。
* 有氧训练显示项目、时长和心率。
* 导出全部 CSV。
* 导出全部 JSON。
* 清空本地数据，带确认弹窗。

当前版本没有单次历史详情页，也没有图表。

## 6. 当前架构

### 6.1 计划解析模块

对应实现：
* `src/domain/planSchema.ts`
* `src/samplePlans/currentTrainingPlan.json`
* `src/domain/localization.ts`

职责：
* 校验训练计划 JSON。
* 接受合法计划，拒绝缺字段或结构错误的计划。
* 保留计划中的现实训练表达，例如 两边各7.5kg、辅助33kg、心率130-140。
* 对旧版英文示例计划做已知文案迁移，避免用户本地已有数据时页面继续显示英文。

### 6.2 训练执行模块

对应实现：
* `src/domain/workoutRunner.ts`
* `src/appLogic.ts`
* `src/components/WorkoutScreen.tsx`

职责：
* 将训练日展开成顺序执行的训练组.
* 维护未完成训练状态。
* 完成当前组、进入休息、记录实际次数。
* 休息结束后由用户手动进入下一组。
* 最后一组完成后保存训练会话。
* 最后一组的修正时间不计入实际休息时长。

### 6.3 提醒模块

对应实现：
* `src/reminders/reminder.ts`

职责：
* 休息结束时尝试播放短声音。
* 休息结束时尝试触发震动。
* 如果音频或震动 API 不可用，页面状态仍然能提示用户。

倒计时显示通过时间戳计算剩余秒数，不依赖浏览器持续执行间隔。

### 6.4 本地存储模块

对应实现：
* `src/storage/db.ts`

职责：
* 保存当前训练计划。
* 保存历史训练会话。
* 保存未完成训练。
* 读取历史时按 `startedAt` 和 `sessionId` 稳定倒序排序。
* 清空本地计划、历史和未完成训练。

数据库使用 IndexedDB，当前版本没有服务器，也没有云同步。

### 6.5 导出模块

对应实现：
* `src/domain/exporters.ts`

职责：
* 导出当前训练或全部历史为 JSON。
* 导出当前训练或全部历史为 CSV。
* 正确处理中文、逗号、引号和换行。
* CSV 字段名前缀保持英文 snake_case，便于后续用脚本或表格工具分析。

### 6.6 提示模块

对应实现：
* `src/domain/hints.ts`

职责：
* 基于历史顶组记录生成简单提示。
* 当前规则：同一动作最近两次顶组都达到目标上限时，提示可考虑小幅加重量或降低辅助。
* 只给建议，不修改计划。

## 7. 训练计划 JSON 结构

当前导入格式示例：

```json
{
  "version": 1,
  "planName": "8周训练计划 2026-05",
  "days": [
    {
      "id": "strength-a",
      "name": "力量A",
      "focus": "上肢推拉 + 下肢中等强度",
      "blocks": [
        {
          "exerciseId": "smith-bench",
          "name": "史密斯卧推",
          "primaryMuscles": ["胸大肌"],
          "secondaryMuscles": ["肱三头肌", "三角肌前束"],
          "progressionHint": "顶组达到 8 次后可考虑小幅加重。",
          "sets": [
            {
              "kind": "top",
              "plannedWeight": "两边各7.5kg",
              "targetReps": "6-8",
              "restSeconds": 180
            }
          ]
        }
      ]
    }
  ]
}
必填字段：

根节点：version、planName、days

训练日：id、name、blocks

动作块：exerciseId、name、sets

训练组：kind、plannedWeight、targetReps、restSeconds

可选字段：

focus

primaryMuscles

secondaryMuscles

notes

progressionHint

支持的 kind：

top

backoff

working

accessory

cardio

8. 训练记录结构
8.1 力量训练组记录
每完成一组力量训练，会创建一条 SetLog：

JSON
{
  "sessionId": "strength-a-2026-05-17T20:00:00+08:00",
  "dayId": "strength-a",
  "dayName": "力量A",
  "exerciseId": "smith-bench",
  "exerciseName": "史密斯卧推",
  "setIndex": 1,
  "setKind": "top",
  "plannedWeight": "两边各7.5kg",
  "targetReps": "6-8",
  "actualReps": 8,
  "plannedRestSeconds": 180,
  "actualRestSeconds": 165,
  "completedAt": "2026-05-17T20:05:00+08:00"
}
8.2 有氧记录
有氧训练保存为 CardioLog：

JSON
{
  "sessionId": "cardio-2026-05-17T20:00:00+08:00",
  "dayId": "cardio",
  "dayName": "有氧",
  "activity": "爬坡跑步机",
  "targetDurationMinutes": "30-45分钟",
  "targetHeartRate": "心率130-140",
  "actualDurationMinutes": 40,
  "actualHeartRate": "132-142",
  "notes": "状态稳定",
  "completedAt": "2026-05-17T20:45:00+08:00"
}
8.3 会话记录
一次训练保存为 WorkoutSession，包含：

sessionId

planName

planVersion

dayId

dayName

startedAt

finishedAt

setLogs

cardioLog

力量训练使用 setLogs；有氧训练使用 cardioLog。

9. 错误与恢复
9.1 计划导入失败
如果 JSON 无法解析或结构不合法，显示错误信息，并保留之前有效的计划。

当前实现会显示元字段路径和中文错误说明，例如：

Plaintext
days.0.blocks.0.sets.0.plannedWeight: 训练计划格式不符合要求
9.2 未完成训练恢复
如果用户刷新、关闭页面或回来时存在未完成训练，系统会提示：

是否继续未完成训练。

如果已完成减组，是否保存已完成组并结束这次训练。

是否删除这次未完成训练。

9.3 休息计时可靠性
休息计时基于 restStartedAt 和 restEndsAt。页面恢复后会用当前时间重新计算剩余秒数，以降低手机浏览器后台节流造成的影响。

9.4 提醒失败
声音或震动可能因为浏览器权限、系统设置或后台状态失败。即使失败，页面仍显示休息完成。

9.5 实际次数修正
用户可以在当前训练的休息阶段修正刚完成一组的实际次数。当前版本不支持任意编辑历史训练。

9.6 计划更新
重新导入计划不会修改已有训练记录。每条记录保存当时使用的计划重量、目标次数和动作名称。

10. 已验证测试范围
当前自动化测试覆盖：

计划解析：合法计划、示例计划、缺字段、非法休息时间。

训练执行：展开训练日、创建训练、完成组、休息、进入下一组、完成最后一组。

最后一组修正时间不计入实际休息秒数。

导出：CSV 字段顺序、转义、JSON 输出、非法时间错误。

提示：最近两次顶组达到目标上限时生成中文提示。

本地存储：计划、未完成训练、历史训练、清空数据、数据库版本迁移。

提醒：音频和上下文关闭和震动返回。

页面：中文首页、中文组类型显示。